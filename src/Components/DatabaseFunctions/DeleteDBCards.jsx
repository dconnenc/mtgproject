export const DeleteDBCards = () => {
    const deleteDBCards = () => {
        try {
            
        } catch (error) {
            
        }
    }
    
    
    return(
        <>
            <button onClick={deleteDBCards}> Delete Cards </button>
        </>
    )
}